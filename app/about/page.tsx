import SectionContainer from '@/components/SectionContainer';
import ScrollTop from '@/components/posts/ScrollTop';

export default async function About() {
  return (
    <SectionContainer>
      <ScrollTop />
      <div>Waiting For Being Done.</div>
    </SectionContainer>
  );
}
